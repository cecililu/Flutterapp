
import 'package:flutter/material.dart';
void main(){
   runApp (Myapp());
 }
 

// ignore: use_key_in_widget_constructors
class Myapp extends StatelessWidget {
  
     Widget build(BuildContext ctx){
       return MaterialApp(home: Text('Hello, cecil! How are you?',));
   }

 }